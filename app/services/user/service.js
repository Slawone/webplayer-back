/**
 * @typedef {import('./types/regData').RegDataOpts} RegDataOpts
 */

/**
 * @function getRegData
 * @param {RegDataOpts} regData
 * @returns {object} result
 */

export const getRegData = (regData) => {
  return regData;
  // throw new Error('Data not found');
};
